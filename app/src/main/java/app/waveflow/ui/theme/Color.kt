package app.waveflow.ui.theme

import androidx.compose.ui.graphics.Color

// Palette WaveFlow — accent émeraude (preset par défaut du desktop).
val Emerald = Color(0xFF10B981)
val EmeraldDark = Color(0xFF059669)
val EmeraldLight = Color(0xFF6EE7B7)

// Surfaces sombres (dark-first, façon Apple Music / WaveFlow).
val SurfaceDark = Color(0xFF121212)
val SurfaceDarkElevated = Color(0xFF1E1E1E)
val OnDark = Color(0xFFECECEC)
val OnDarkVariant = Color(0xFFA1A1AA)

// Surfaces claires.
val SurfaceLight = Color(0xFFFAFAFA)
val OnLight = Color(0xFF18181B)
val OnLightVariant = Color(0xFF52525B)
